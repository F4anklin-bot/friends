{
  "name": "friends",
  "private": true,
  "scripts": {
    "dev": "npx --yes concurrently -n web,api -c magenta,cyan \"npm run dev --prefix frontend\" \"npm run dev --prefix server\"",
    "dev:web": "npm run dev --prefix frontend",
    "dev:api": "npm run dev --prefix server"
  }
}
